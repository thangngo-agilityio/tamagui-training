'use client';

import { OrderSuccess } from '@/components';
import { OverviewSection } from '@/ui/section';

const OrderPage = () => (
  <>
    <OverviewSection />
    <OrderSuccess />
  </>
);

export default OrderPage;
